dbs:
  - path: /data/agentcockpit.db
    replicas:
      - type: gcs
        bucket: ${GCS_BUCKET}
        path: agentcockpit/db
        # Sync WAL every 1s; retain 24h of history
        sync-interval: 1s
        retention: 24h
        retention-check-interval: 1h
